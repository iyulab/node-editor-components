name: Cleanup Expired Artifacts

on:
  workflow_dispatch:
  schedule:
    - cron: "0 3 * * 0"

jobs:
  cleanup:
    uses: iyulab/shared/.github/workflows/cleanup-expired-artifacts.yml@1802449344d6384c28bfe8706e28a822e8a3fd9c
